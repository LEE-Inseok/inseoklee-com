---
import BaseLayout from '../../layouts/BaseLayout.astro';
import CategoryNav from '../../components/CategoryNav.astro';
import { getCollection } from 'astro:content';

// Projects render straight from the `development` collection, with the same
// status rule as Research panels: published → linked card, draft → linked +
// "In progress", coming-soon → unlinked dimmed stub. Entries with
// `externalUrl` link out (e.g. GitHub) instead of to a detail page.
const statusOrder: Record<string, number> = { 'published': 0, 'draft': 1, 'coming-soon': 2 };
const projects = (await getCollection('development')).sort(
  (a, b) =>
    (statusOrder[a.data.status] ?? 9) - (statusOrder[b.data.status] ?? 9) ||
    (a.data.date ?? '').localeCompare(b.data.date ?? '')
);

const navGroups = [
  {
    key: 'agent',
    label: 'AI Agent Engineering',
    trigger: 'activate' as const,
    activateView: 'agent',
  },
  {
    key: 'projects',
    label: 'Projects',
    trigger: 'activate' as const,
    activateView: 'projects',
  },
];

// AI · Workflow — three tiers of AI usage
const workflowItems = [
  {
    title: 'Claude',
    summary: 'General chat for ideation, quick research questions, drafting, and lightweight summarisation — no codebase context, no tool-use.',
  },
  {
    title: 'Claude Code',
    summary: 'Coding work in VS Code extension or the desktop app — file edits, repo-aware refactoring, terminal-integrated debugging, and review loops.',
  },
  {
    title: 'Multi-Agent Orchestration',
    summary: 'Three-stage execution: Plan Mode → Codex cross-validation → specialist subagents (developer / debugger / analyst / tester / verifier) running the implementation.',
  },
];

// AI · Harness Engineering — tooling that customises the Claude harness
const harnessItems = [
  {
    title: 'MCP Servers',
    summary: 'Model Context Protocol servers connecting Claude to external systems — filesystem, browser, EndNote, Notion, Google Drive, and scheduled tasks.',
  },
  {
    title: 'Skills',
    summary: 'Reusable capability bundles auto-loaded per task — PDF, DOCX, XLSX, PPTX, design review, research synthesis, and project-specific helpers.',
  },
  {
    title: 'Hooks',
    summary: 'Event-driven automations along the agent lifecycle — pre/post tool, session start/stop, prompt submission gates, and stop-time review hooks.',
  },
  {
    title: 'Subagents',
    summary: 'Specialist Claude agents (developer / debugger / architect / researcher / analyst / tester / tech-writer / verifier) coordinated by an orchestrator.',
  },
  {
    title: 'Slash Commands',
    summary: 'Project-scoped commands that wrap recurring workflows — /codex:rescue, /codex:review, /research-toolkit, /sync-papers, /portfolio-update, etc.',
  },
];
---

<BaseLayout
  title="AI Engineering — Inseok Lee"
  description="Building and directing AI agents for engineering — custom harnesses, multi-agent orchestration, and the tools built with them."
  section="development"
>
  <div class="container-site py-16">
    <header class="mb-12 border-b pb-8" style="border-color: var(--border);">
      <span class="section-label">AI Engineering</span>
      <h1 class="text-section mt-3" style="color: var(--ink); font-family: 'Fraunces', Georgia, serif;">Agents. Automation. Outcomes.</h1>
      <p class="mt-3 text-base max-w-xl" style="color: var(--muted);">
        Engineering tools, automation frameworks, and productivity systems built to accelerate research and simulation workflows.
      </p>
    </header>

    <div class="flex flex-col lg:flex-row lg:gap-12">

      <aside class="lg:w-56 flex-shrink-0 mb-8 lg:mb-0">
        <div class="lg:sticky lg:top-24">
          <CategoryNav
            groups={navGroups}
            defaultView="agent"
            ariaLabel="AI Engineering categories"
          />
        </div>
      </aside>

      <div class="flex-1 min-w-0">

        <!-- AI Agent Engineering — the harness I built + how I run it -->
        <section data-view-panel="agent" hidden aria-labelledby="cn-agent-trigger">
          <h2 class="view-title">AI Agent Engineering</h2>
          <p class="max-w-2xl" style="color: var(--muted); margin-bottom: 2.25rem;">
            I don't just use AI — I build a custom agent setup and direct it to do engineering work.
            Two parts: the tooling I built, and how I run it.
          </p>

          <h3 class="sub-title">What I Built</h3>
          <div class="flex flex-col max-w-2xl" style="margin-bottom: 2.75rem;">
            {harnessItems.map((p, i) => (
              <div class="card flex gap-5" data-reveal style={`transition-delay: ${i * 70}ms`}>
                <span class="row-num" aria-hidden="true">{String(i + 1).padStart(2, '0')}</span>
                <div class="flex flex-col gap-3 min-w-0">
                  <h4 class="card-title">{p.title}</h4>
                  <p class="text-base" style="color: var(--body);">{p.summary}</p>
                </div>
              </div>
            ))}
          </div>

          <h3 class="sub-title">How I run</h3>
          <div class="flex flex-col max-w-2xl">
            {workflowItems.map((p, i) => (
              <div class="card flex gap-5" data-reveal style={`transition-delay: ${i * 70}ms`}>
                <span class="row-num" aria-hidden="true">{String(i + 1).padStart(2, '0')}</span>
                <div class="flex flex-col gap-3 min-w-0">
                  <h4 class="card-title">{p.title}</h4>
                  <p class="text-base" style="color: var(--body);">{p.summary}</p>
                </div>
              </div>
            ))}
          </div>
        </section>

        <!-- Projects -->
        <section data-view-panel="projects" hidden aria-labelledby="cn-projects-trigger">
          <h2 class="view-title">Projects</h2>
          <div class="flex flex-col max-w-2xl">
            {projects.length === 0 && (
              <p class="text-base" style="color: var(--muted);">No projects recorded yet.</p>
            )}
            {projects.map((entry, i) => {
              const num = String(i + 1).padStart(2, '0');
              if (entry.data.status === 'coming-soon') {
                return (
                  <div class="card flex gap-5" data-stub="true" data-reveal style={`transition-delay: ${i * 70}ms`}>
                    <span class="row-num" aria-hidden="true">{num}</span>
                    <div class="flex flex-col gap-3 min-w-0">
                      <span class="section-label" style="color: var(--muted);">Coming soon</span>
                      <h4 class="card-title" style="color: var(--muted);">{entry.data.title}</h4>
                      <p class="text-base" style="color: var(--muted);">{entry.data.summary}</p>
                    </div>
                  </div>
                );
              }
              const isExternal = !!entry.data.externalUrl;
              const href = entry.data.externalUrl ?? `/development/${entry.slug}`;
              return (
                <a
                  href={href}
                  class="card flex gap-5 group"
                  data-reveal
                  style={`transition-delay: ${i * 70}ms`}
                  {...(isExternal ? { target: '_blank', rel: 'noopener noreferrer' } : {})}
                >
                  <span class="row-num" aria-hidden="true">{num}</span>
                  <div class="flex flex-col gap-3 min-w-0">
                    {entry.data.status === 'draft' && (
                      <span class="section-label" style="color: var(--accent-dk);">In progress</span>
                    )}
                    <h4 class="card-title">{entry.data.title}</h4>
                    <p class="text-base" style="color: var(--body);">{entry.data.summary}</p>
                    {entry.data.tags && (
                      <div class="flex flex-wrap mt-1">
                        {entry.data.tags.map(tag => <span class="chip">{tag}</span>)}
                      </div>
                    )}
                    <span class="text-sm mt-1" style="color: var(--accent-dk);">{isExternal ? 'View on GitHub ↗' : 'View project →'}</span>
                  </div>
                </a>
              );
            })}
          </div>
        </section>

      </div>
    </div>
  </div>
</BaseLayout>

<style>
  .view-title {
    font-size: 1.75rem;
    font-family: 'Fraunces', Georgia, serif;
    font-weight: 600;
    color: var(--ink);
    margin-bottom: 1.5rem;
  }
  .sub-title {
    font-size: 0.8rem;
    font-weight: 600;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--muted);
    margin-bottom: 1.25rem;
  }
  .card-title {
    font-size: 1.25rem;
    font-family: 'Fraunces', Georgia, serif;
    font-weight: 600;
    color: var(--ink);
  }
  [data-view-panel] {
    transition: opacity 150ms ease;
  }
  [data-view-panel][hidden] {
    display: none;
  }
  [data-stub="true"] {
    opacity: 0.65;
  }
</style>
